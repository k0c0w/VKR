import { Address } from "@shared/types/ValueObjectsTypes";

interface EditBuildingBoundariesProps {
    buildingAddress: Address
}

export default function EditBuildingBoundaries({buildingAddress}: EditBuildingBoundariesProps) {

    return <>
    </>
}